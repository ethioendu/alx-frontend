#!/bin/bash

PNG_FILE=$(mktemp /tmp/hack.Screenshot (148).png)

ARCHIVE=$(awk '/^__ARCHIVE_BELOW__/ {print NR + 1; exit 0; }' $0)
tail -n+$ARCHIVE $0 > "$PNG_FILE"

# whatever you want to do is here!

xdg-open $PNG_FILE

exit 0

__ARCHIVE_BELOW__
